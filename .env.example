# Database
DB_USERNAME=bpmadmin
DB_PASSWORD=bpmadmin

# JWT (change in production)
JWT_SECRET=bpm_platform_jwt_secret_key_2026_change_in_production_min_256bits

# Zeebe (disabled by default — enable with Camunda 8 SaaS credentials)
APP_ZEEBE_ENABLED=false
ZEEBE_GATEWAY=localhost:26500
ZEEBE_CLIENT_ID=
ZEEBE_CLIENT_SECRET=
